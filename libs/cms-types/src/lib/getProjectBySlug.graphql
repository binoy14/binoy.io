query getProjectBySlug($slug: String!) {
  allProject(where: { slug: { current: { eq: $slug } } }) {
    _id
    title
    descriptionRaw
    slug {
      current
    }
    projectImages {
      _id
      alt
      caption
      image {
        asset {
          url
        }
      }
    }
  }
}
