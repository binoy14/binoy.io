query getProjectBySlug($slug: String!) {
  allProject(where: { slug: { current: { eq: $slug } } }) {
    _id
    title
    description
    slug {
      current
    }
    projectImages {
      _id
      alt
      caption
      image {
        asset {
          url
        }
      }
    }
  }
}
